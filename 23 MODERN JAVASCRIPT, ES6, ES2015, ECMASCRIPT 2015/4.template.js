const firstName = 'Sazidul';
const lastName = 'Alam';

const fullName = `${firstName} ${lastName} is a good boy.`;
console.log(fullName);

const message = `this is first line
this is second line
this is last line.`

console.log(message);